CREATE SCHEMA public."Atividades"
-- Table: public."Atividades"

-- DROP TABLE public."Atividades";

CREATE TABLE public."Atividades"
(
    "ID Atividades" "char[]" NOT NULL,
    "Horas" abstime,
    CONSTRAINT "Atividades_pkey" PRIMARY KEY ("ID Atividades")
)
WITH (
    OIDS = FALSE
)
TABLESPACE pg_default;

ALTER TABLE public."Atividades"
    OWNER to postgres;

-- Table: public."Processo"

-- DROP TABLE public."Processo";

CREATE TABLE public."processos"
(
    "ID" serial NOT NULL,
    "atividade" "varchar",
    "matricula" "varchar",
    "descricao" "varchar",
    "data_abertura" date,
    "horas" integer,
    "certificado" "varchar" NOT NULL,
    "validação" boolean,
    CONSTRAINT "Processo_pkey" PRIMARY KEY ("ID"),
	CONSTRAINT "Atividades_pkey" FOREIGN KEY ("atividade") REFERENCES atividades ("codigo"),
	CONSTRAINT "Usuário_pkey" FOREIGN KEY ("matricula") REFERENCES usuarios ("matricula")
)
WITH (
    OIDS = FALSE
)
TABLESPACE pg_default;

ALTER TABLE public."Processo"
    OWNER to postgres;

-- Table: public."Usuário"

-- DROP TABLE public."Usuário";

CREATE TABLE public."Usuário"
(
    "Matrícula" "char[]" NOT NULL,
    "Senha" "char[]",
    "Nome usuário" "char[]",
    "Tipo usuário" integer,
    CONSTRAINT "Usuário_pkey" PRIMARY KEY ("Matrícula")
)
WITH (
    OIDS = FALSE
)
TABLESPACE pg_default;

ALTER TABLE public."Usuário"
    OWNER to postgres;